######
```c
#define STR(x) #x

printf("%s\n", STR(hello));     // 打印 hello
printf("%s\n", STR(1 + 2));     // 打印 1 + 2,原样,不计算
```

最常用的写法 - 连变量名一起打印出来,省得手写:

```c
#define PRINT_INT(x) printf(#x " = %d\n", x)

int score = 95;
PRINT_INT(score);               // 输出: score = 95
```


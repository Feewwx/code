#####
```c
#include <stdio.h>

enum color { RED, GREEN, BLUE };

enum sex { MALE = 1, FEMALE };

int main() {
    printf("%d\n", RED);     // 0
    printf("%d\n", GREEN);   // 1
    printf("%d\n", BLUE);    // 2

    printf("%d\n", MALE);    // 1
    printf("%d\n", FEMALE);  // 2
    return 0;
}
```


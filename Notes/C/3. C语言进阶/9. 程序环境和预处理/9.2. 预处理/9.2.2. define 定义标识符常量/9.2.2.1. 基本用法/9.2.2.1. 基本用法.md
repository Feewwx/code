######
```c
#define MAX 100
#define NAME "fewx"

int main() {
    int arr[MAX];                  // 能用,预处理阶段已经换成字面量 100
    printf("%d %s\n", MAX, NAME);
    return 0;
}
```

`MAX` 到编译器那儿已经变成字面量 `100` 了,类型是 `100` 自己的类型(int),跟 define 没关系.


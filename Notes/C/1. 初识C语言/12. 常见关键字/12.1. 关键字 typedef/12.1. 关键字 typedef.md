####
`typedef` 用于为类型起别名

```c
#include <stdio.h>

typedef unsigned int uint;
typedef struct Node {
    int data;
    struct Node *next;
} Node;

int main() {
    uint num = 1;  // 等价于 unsigned int
    Node n;        // 等价于 struct Node n
    return 0;
}
```

#####
预处理指令 `#define`
格式: `#define <标识符> <常量>`

```c
#include <stdio.h>

#define MAX 100
#define STR "hello"

int main() {
    printf("MAX = %d\n", MAX);
    int a = MAX;
    printf("a = %d\n", a);
    printf("STR = %s\n", STR);
    MAX = 200;  // err,标识符常量不可修改
    return 0;
}
```

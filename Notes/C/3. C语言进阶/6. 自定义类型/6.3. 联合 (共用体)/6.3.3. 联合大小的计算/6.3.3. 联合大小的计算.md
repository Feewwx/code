#####
```c
#include <stdio.h>

union Un {
    char arr[5];
    int i;
};

int main() {
    printf("%d\n", sizeof(union Un));  // 8
    return 0;
}
```

- 联合也存在内存对齐,规则和结构体一样

- 唯一不同的是联合所有成员共用一块空间,占用的大小只会是最大的成员的大小
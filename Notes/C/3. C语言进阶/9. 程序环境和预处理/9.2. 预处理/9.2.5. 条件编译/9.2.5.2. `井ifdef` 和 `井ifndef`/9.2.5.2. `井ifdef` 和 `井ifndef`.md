######
只看"这个名字定义过没有",不管它的值是多少:

```c
#ifdef  名字     // 定义过 → 留下
#ifndef 名字     // 没定义过 → 留下
```

注意一个坑:**没定义过的宏,在 `#if` 里一律当成 0.**

```c
#if FOO          // FOO 没定义 → 当成 0 → 整段丢掉
    ...
#endif

#define FOO 1    // 写在这儿太晚了,上面早算完了
```


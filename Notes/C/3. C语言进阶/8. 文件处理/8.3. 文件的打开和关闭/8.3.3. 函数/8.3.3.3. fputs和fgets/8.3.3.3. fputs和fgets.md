######
```c
fputs("hello world\n", pf);  // 写一整行(不会自动补 \n,记得自己加)

char line[100];
fgets(line, sizeof(line), pf);  // 读一整行(最多 99 字符,带 \n 一起存进 line)
printf("%s", line);             // fgets 存了 \n,所以这里别再加 \n
```


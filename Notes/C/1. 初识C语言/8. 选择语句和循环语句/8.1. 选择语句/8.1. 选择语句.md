####
**选择语句:** `if` 语句 `switch` 语句

```c
#include <stdio.h>

int main() {
    int input = 0;
    printf("进入大学\n");
    printf("要好好学习吗? (1-是 0-否): ");
    scanf("%d", &input);
    if (input == 1) {
        printf("好offer\n");
    } else {
        printf("卖红薯\n");
    }
    return 0;
}
```
